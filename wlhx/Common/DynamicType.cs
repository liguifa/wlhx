using System;
using System.Collections.Generic;
using System.Linq;
using System.Web;

namespace wlhx.Common
{
    public enum DynamicTypes
    {
        News = (int)1,
        Toast = (int)2,
        Experiment = (int)3,
        Achievement = (int)4,
        File = (int)5,
        Teacher = (int)6,
        TeamInfo = (int)7,
        ExperimentResource = (int)8
    }
}